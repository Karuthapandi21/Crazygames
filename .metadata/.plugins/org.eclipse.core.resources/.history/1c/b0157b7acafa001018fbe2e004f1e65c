package org.gemini;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

public class LogIn extends Crazygames {
	WebDriver h;
	@BeforeMethod
	public void method() {
		
	}
	@Test
public void loginButton(WebDriver driver) throws InterruptedException {
		WebDriver h=driver;
		h.manage().timeouts().implicitlyWait(5, TimeUnit.SECONDS);
		h.findElement(By.xpath("//button[@id='header-login-button']")).click();
		WebElement element2 = h.findElement(By.id("email"));
		element2.click();
		element2.sendKeys("");
		h.findElement(By.xpath("//button[@type='submit']")).click();
		
}
	@AfterMethod
	public void function() {
		
	}
	
}
